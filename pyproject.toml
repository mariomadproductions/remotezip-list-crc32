[project]
name = "remotezip-list-crc32"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.14.3",
    "fsspec>=2026.9.0",
    "py7zr>=1.1.3",
    "remotezip>=0.12.3",
    "requests>=2.33.1",
]

[project.scripts]
remotezip-list-crc32 = "remotezip_list_crc32:main"

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
